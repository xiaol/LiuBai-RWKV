# RWKV-7 Song LM — Status log

Chronological record of what was run, measured and decided (moved out of PLAN.md §9 on 2026-09-20; PLAN.md keeps the design
and a short current-state summary). Newest entry last. Dates are local (CST). Every number here has a log or json path next to it.


- **2026-09-12** Shard 0 of suno-94k tokenized: 1,135 clips, 0 failures, 64.4 h audio, 12.8 min
  on one A100 (302× realtime), 177 MiB npz. Stats: median song 203 s (10.2k cb0 tokens), p95 327 s
  (16.4k tokens → exceeds a 16k context: truncate or drop >5 min songs for stage-1), lyrics
  median 1,072 chars, script mix 85 % Latin / 6 % Cyrillic / 6 % CJK, 5,054 distinct caption tags
  in one shard, 10 % of songs share exact lyrics with another song (dedupe by lyric hash), 1,091
  distinct creators (no creator dominates). Full run launched: shards 1–42 on GPU 0, 43–84 on GPU 1
  (`logs/prep_suno94k_gpu{0,1}.log`); expected ≈ 9–10 h. Tokens ≈ 15 GB total, tars deleted as they finish.
- **2026-09-12 22:15** Progress check: 19/85 shards done (0–9, 43–52), 0 clip failures, 13.2 min tokenize
  per shard, but the wall-clock cycle was 21–23 min and rising because the worker downloads each tar
  serially and the HF CDN edge degraded to <1 MB/s (see §0 caveat). Pinned the fast edge in `/etc/hosts`
  (≈100 MB/s per download again) and launched `scripts/prefetch_shards.py`. Remaining 66 shards at the
  13-min tokenize cycle → done ≈ 05:30 on 2026-09-13. Disk: 467 GB free, steady-state raw audio <25 GB;
  user will mount additional disk before the humair025 / MTG-Jamendo phases. Leftover from the shard-0
  test run: `data/raw/suno94k/suno-various-94k-00000.tar` (5.4 GB), safe to delete.
- **2026-09-13 09:20** Overnight run finished 80/85 shards with 0 clip failures (14 GB of npz). Shard 84 is
  the half-size last shard (570 clips; index lists 1,140 files = mp3+json pairs), not a truncation. Shards
  28, 29, 71, 72, 73 were skipped: a ~1 min CDN blip around 03:15 made the worker's own `download()`
  exhaust its 3 retries (10/20/30 s backoff) before the prefetcher had those tars. Relaunched
  `run_all_shards.sh` (resumable; now appends to logs instead of truncating — the overnight log text was
  lost to the old `>`), GPUs 0/1 back at 100 %; expected complete ≈ 10:05. Prefetcher still running and
  will exit on its own. Follow-ups: raise worker download retries / backoff, then `dataset_report.py`.
- **2026-09-13 14:00** suno-94k complete: 85/85 shards, 94,174 songs, 0 failures, 5,423 h, 976 M cb0
  tokens, 15 GB npz, all arrays validated. Full-corpus stats: duration p50 205 s / p95 334 s, 8,819 songs
  > 300 s, 817 < 30 s; lyrics 87 % Latin / 5.5 % Cyrillic / 5.6 % CJK; ~11.7k instrumental-only; 53k
  creators (top share 0.5 %); Suno versions auk 37 % / fenix 34 % / crow 16 %. Stage-1 manifest:
  84,500 kept (dropped 8,819 too long, 817 too short, 38 over ctx), **train 83,714 songs / 4,500 h /
  845 M tokens (810 M cb0 + 35 M text)**, val 775 songs / 7.8 M tokens (11 removed for lyric overlap
  with train). Doc length p50 10,343 / p95 14,633; text p50 395 tokens. binidx written and spot-checked
  (decoded text == manifest text, cb0 == npz, control tokens in place). `magic_prime` train = 51,563,
  val = 467. Download retries in `prepare_suno94k.py` raised 3 → 10. Shard-0 test tar deleted.
  **Next (phase 3):** resize the g1k checkpoint to vocab 67,072 (`scripts/resize_vocab.py`, to write),
  then a smoke run of RWKV-LM `train.py` on 4 GPUs: `--data_type binidx --vocab_size 67072 --ctx_len 16384
  --magic_prime 51563`, note that `epoch_steps × real_bsz` must equal 40,320 (RWKV-LM assert), so with
  4 GPUs × micro_bsz 1 use `--epoch_steps 10080`. The extra corpora (humair025, MTG-Jamendo) wait for
  the additional disk the user is mounting.
- **2026-09-13 16:15 Stage-1 training launched** (`scripts/train_stage1.sh 0,1,2,3`, log
  `logs/train_stage1_20260913_160907.log`, per-step loss in `out/stage1/step_log.txt`: columns
  step, loss, lr, kt/s, Gtokens, time). Getting there took: pytorch-lightning 1.9.5 + deepspeed 0.16.9
  install, `resize_vocab.py` (emb/head → 67,072 rows), and four local patches to RWKV-LM train_temp
  (LoRA-dim env overrides, sm_80 atomics in 3 kernels, compile-time vocab for the chunked head-CE kernel,
  step logging in `trainer.py`; originals saved as `*.orig`, details in §3). Two false starts: the first
  launch died on the head-CE kernel's hard-coded vocab 65,536; the second ran but logged nothing per step.
  Measured: 4 × A100, micro_bsz 1, ctx 16,384, ZeRO-2 + grad_cp + head_chunk 65,536 → **25.6–27 GB per GPU,
  12.6 k tokens/s total (5.2 s per 65,536-token step)**. That is ≈ 18.6 h per pass over the 845 M-token
  train set and ≈ 37 h to the 1.69 B-token cosine horizon (`rwkv-final.pth`); first checkpoint
  `rwkv-0.pth` after 10,080 steps ≈ 14.5 h. Loss: 16.4 at step 1 → 11.8 at step 10 (new head rows learning
  the audio unigram), no NaN. Speed levers if wanted later: micro_bsz 2 (memory headroom ~13 GB), the
  `@rwkv3` kernel variant, or dropping grad_cp on the last layers. To watch: `tail out/stage1/step_log.txt`;
  to resume after a crash: rerun `train_stage1.sh` (train_stage 3 picks the newest `out/stage1/rwkv-*.pth`).
  **Next:** an eval script over `data/binidx/suno94k_stage1_val` (cb0 loss by position, text vs audio) to
  run on `rwkv-0.pth`, then a 20-song generation sweep through YuE1 stage-2 + X-Codec decode (§4).
- **2026-09-14 08:30 Run 1 diverged; run 2 launched.** Loss fell 16.4 → 6.1 (step 60) → 4.5 (first 500)
  → 3.30 (steps 3500–4640, still improving), then **NaN at step 4645** (23:00) with no spike beforehand
  (max loss after step 1000 was 3.89) and nothing unusual in the 36 windows around it (checked token ids,
  runs, text/audio mix). LR was 9.4e-5, just past the 1e-4 peak. bf16 ZeRO-2 has no overflow skip, so one
  inf gradient turned 1,059/1,062 tensors NaN; the run then burned 9 h producing NaN (moved to
  `out/stage1/nan-rwkv-0.pth`, no usable checkpoint — the only save was the 10,080-step epoch save).
  Fixes for run 2: peak LR **6e-5 → 6e-6**; `trainer.py` saves `step-N.pth` every 1,000 steps (≈ 1.4 h,
  keeps 2) and kills the run on a non-finite loss; `scripts/train_stage1.sh` is now a supervisor that resumes
  from the newest `step-N.pth` with `RWKV_STEP_OFFSET=N` (LR schedule, warmup and data windows continue
  from N; `dataset.py` patched), uses `train_stage 0` so `train.py` does not override `--load_model`, and
  stops only when `rwkv-final.pth` (cosine horizon) exists. The trainer's own one-pass save was renamed
  `rwkv-onepass.pth`. Events (saves, NaN kills, restarts) go to `out/stage1/events.txt`. Run 2 restarts from
  `rwkv-init.pth` (7 h of good training lost). ETA at 12.6 k tok/s: one pass ≈ 18.6 h, horizon ≈ 37 h → ~21:30 on 2026-09-15.
- **Disk plan (2026-09-14):** 450 GB free. Project footprint now 47 GB (tokens 15, binidx 3.2, models 16,
  out 12). Growth if the plan is followed with streaming (raw audio deleted after tokenizing): stage-1
  checkpoints ≤ 6 GB each, keep ~5 → 30 GB; humair025 tokens ≈ 7 GB; MTG-Jamendo tokens ≈ 8 GB; YuE1 stage-2
  + decoders ≈ 3 GB; generated audio negligible → **≈ 100 GB total, fits without the new disk.** Keeping raw
  audio would need 454 + 213 + 117 ≈ 785 GB → that is what the extra disk is for, if wanted.
- **2026-09-14** Reviewed YuE2 for reuse (`docs/yue2_reference/`): no audio→semantic-token encoder is released (inference-only pipeline), the NAR only renders YuE2's own 32,768-entry semantic tokens, and all weights are CC BY-NC 4.0. Decision at the time: stay on X-Codec + YuE1 stage-2 (Apache-2.0); YuE2 evaluation-only. Added License row to §0 and renderer note to §5.
- **2026-09-14 (later)** User decision: **target YuE2-level quality, licensing deferred, experiments first.** Added §7 (Track B inverse-tokenizer distillation, Track A own NAR → YuE2-Vae, GPU schedule), rewrote §0 Audio-tokens / Renderer / License rows, marked §4 YuE1 path as baseline. Stage-1 cb0 run 2 left running (step 5160, loss 3.29 at 16:06). YuE2 weights still to download. Status log renumbered to §8.
- **2026-09-14 18:10 Generation-2 design (§8).** User goal restated: build on YuE2 with the RWKV backbone and surpass YuE2 and Suno v6. Researched: Suno v6 family released 2026-09-09 (v6 / v6-wild / v6-mini; 8-min songs, plain-language section edits, mashups, image-to-song, licensed data; scores *below* Suno v5 and YuE2 on WildSongBench per YuE2's page). Found the missing audio→YuE2-token encoder as a third-party release (Mothersuperior v4 head on MERT-v2-FullSong L20, 16.1 % top-1, plus NAR LoRA for real audio and a 4,720-song paired minted corpus); confirmed YuE2's own tokenizer is a causal MERT-v2 branch. Wrote §8 (three axes: lyric/caption adherence + RL, audio-input & state-based editing & unbounded length, Suno-v6 product features; phases G0–G6 with gates). Downloaded YuE2-Vae, YuE2-Vae-legacy, MERT-v2-FullSong to `models/yue2/`; YuE2-3B (7.26 GB) downloading (`HF_HUB_DISABLE_XET=1` required). Stage-1 cb0 run 2: second NaN at step 6,057 (LR 5.4e-5), auto-resumed from `step-6000.pth`, loss 3.03–3.08 at step 6,330. **Next:** G0 — YuE2 venv (torch 2.10), fetch Mothersuperior head/LoRA + minted tokens, round-trip 20 suno val songs.
- **2026-09-14 20:50 G0 round trips work (6 real Suno songs, shard 0).** Setup: `venvs/yue2` (Python 3.12, torch 2.10.0+cu128 aarch64 from download.pytorch.org via aria2c, transformers 4.57.6, `tools/YuE` = yue2-infer 0.1.6 cloned through gh-proxy.com); weights in `models/yue2/` (YuE2-3B, YuE2-Vae, YuE2-Vae-legacy, MERT-v2-FullSong, `mothersuperior_v4/` head + NAR LoRA); shard-0 tar re-downloaded to `data/raw/suno94k/` (kept, 5.4 GB) for real audio. Scripts: `tools/yue2_roundtrip.py` (audio → MERT L20 → v4 head → NAR(±LoRA) → VAE; also writes the VAE-only round trip), `tools/yue2_roundtrip_eval.py` (HeartTranscriptor WER vs lyrics and vs the original's transcript; MERT-v2 recording-embedding cosine), `tools/yue2_mint.py` (B2 minting from suno prompts). Runs fit next to the trainer: ~8.5 GiB peak, 6 songs in 158 s on a shared A100 (NAR 32 steps ≈ 15 s/song). **Results (mean of 6):** WER vs lyrics — original 0.27, VAE-only 0.35, token round trip 0.48 (NAR+LoRA) / 0.44 (stock NAR); WER vs the original's own transcript 0.42 / 0.44; MERT cosine to the original 0.97 for both round trips (VAE-only 0.99, unrelated song 0.64); token repeat rate 0.115; per-frame latent MSE ≈ 1.3 at latent variance ≈ 0.95 (flow matching resamples, so this is not a useful metric). Reading: style/identity survive the 32k-token bottleneck; lyric intelligibility roughly halves with the v4 head (16 % top-1), and the v4 NAR LoRA gives no WER gain on Suno audio. **Gate: on-style yes, intelligible partially → proceed with R1 (better inverse tokenizer) as the first lever; R2 to be re-judged on our own LoRA.** Files: `out/yue2_roundtrip/<id>/{A_original,B_vae_only,C_roundtrip_lora_s32,C_roundtrip_base_s32}.flac` for listening. Running: `yue2_mint.py --n 5` on GPU 0 (true-token ceiling test: render minted songs from true vs predicted tokens), 20-song round trip + eval on GPU 1 (`logs/yue2_roundtrip_n20.log`). Minted-corpus per-track files (`data/yue2_minted/tracks/`) trickle in at 5–80 KB/s (new CDN host `us.aws.cdn.hf.co`, all edges slow); the regularizer pack (all 4,732 token sequences + prompts, 100 MB) is complete.
- **2026-09-14 21:30 G0 confirmed on 20 songs; the inverse tokenizer is the bottleneck, not the renderer.** 20 real Suno songs (shard 0): WER vs lyrics 0.22 original / 0.26 VAE-only / 0.48 (v4 head + v4 NAR LoRA) / 0.46 (v4 head + stock NAR); WER vs the original's transcript 0.17 / 0.44 / 0.44; MERT cosine 0.99 / 0.97 / 0.97 (unrelated song 0.64). **Ceiling test on 5 YuE2-minted songs** (`tools/yue2_mint.py`, cot=off, suno shard-1 prompts, ~1.3× realtime on a shared A100): rendering from the *true* tokens gives WER-vs-original 0.20 and cosine 0.99; from v4-predicted tokens 0.36 and 0.92 (one rap song collapsed: head top-1 0.8 % vs 13–18 % on the others). So NAR+VAE reproduce a song from its tokens nearly losslessly for ASR purposes and the v4 head loses about half the lyric intelligibility → **R1 first.** R1 pipeline built and running: `tools/yue2_extract_mert.py` (MERT-v2-FullSong layers 12/16/20/23 at 25 Hz; corpus tracks are re-synthesised from `latent.npy` with YuE2-Vae, which equals their flac, so the 160 GB audio is not needed), `scripts/train_inverse_tokenizer.py` (112 M params, 4-layer softmax mix → 12-layer bidirectional transformer, 40 s windows, soft-CE over 8 codec-embedding neighbours from `data/yue2_minted/codec_nbr_*.npy`), first run `out/inverse_tok/v1` on GPU 2 (590 corpus tracks + 5 own, 6k steps) — `logs/train_inverse_tok_v1.log`; compare with `yue2_roundtrip.py --head-ckpt out/inverse_tok/v1/best.pt` on the same 20 songs. Overnight: `yue2_mint.py --n 400` on GPU 0 (`data/yue2_minted_own/`, ~2.5 min/song → ~17 h), minted-corpus trickle download, extraction to rerun on new tracks. **Cost:** the cb0 trainer runs at 5–7 k tok/s instead of 12.5 k while GPUs 0–2 are shared (ZeRO waits for the slowest card); its horizon slides accordingly (step 8,300 at 21:09, loss ≈ 3.1–3.3).
- **2026-09-15 11:45 R1 v1 negative, v2 queued; real-audio prep running.** v1 (590 minted tracks, 112 M params, 6k steps): held-out top-1 peaked at **7.1 %** at step 1,750 (top-5 20 %), then overfit (train loss 6.5 → 2.7 while val fell to 6.1 %); on the 20 real songs it is *worse* than the v4 head (WER vs lyrics 0.52 vs 0.48, MERT cosine 0.96 vs 0.97). Expected: v4 trained on 4,765 tracks; exact-match accuracy needs data, not depth. All 4,720 corpus tracks (latent + semantic) finished downloading overnight, plus 264 own minted songs (`data/yue2_minted_own/`, 259 new). MERT features for all of them extracting on GPUs 2/3 (~2,200 tracks each, ~1 h). **v2** queued after that on GPU 2: same model, ~5k tracks, 20k steps, dropout 0.2, 1-s time masking, 10 % feature dropout (`logs/train_inverse_tok_v2.log`). Also started `tools/yue2_prep_real.py --shard 0` on GPU 0: 873 real Suno songs → MERT L12/16/20/23 + true VAE latents + cot=off prefix (`data/yue2_real/shard-00000/`), the inputs for the NAR-teacher flow loss (joint head + NAR-LoRA training, next script). Stage-1 cb0 trainer: step 15,510, loss ≈ 2.95–3.1, back at 12.5 k tok/s when GPUs were free this morning.
- **2026-09-15 12:45 Joint teacher trainer works beside the stage-1 run.** `scripts/train_joint_teacher.py` (port of Mothersuperior's joint.py to our 4-layer head and data layout: real window → head → straight-through codec embeddings → frozen YuE2 AR+NAR (LoRA on the NAR branch, rank 32) → flow-matching MSE against the song's true VAE latents; minted soft-CE anchor; 25 % minted flow windows with true tokens). Smoke test on GPU 1: 10.9 GiB peak at window 256 (10 s) with head+LoRA gradients through 28 checkpointed layers, ~1.5 s/step → fits in the 13 GiB left by the trainer. Held-out real flow loss at start 1.14 (v1 head). Plan: run it with the v2 head as init as soon as v2 finishes; `tools/yue2_prep_real.py` (147/873 songs done at 12:40, ~3.4 s/song) supplies the real windows. Committed as f8bfb32.
- **2026-09-15 21:30 Re-focus: cb0 stage-1 frozen, all GPUs to R1 (§8.6).** User decision: we are on the YuE2 pipeline, focus on the inverse tokenizer and the SOTA RWKV model. Stage-1 cb0 run 2 stopped by hand at step 20,350 (third NaN kill had happened at 17,323, LR 2.0e-5, auto-resumed from step-17000 in 20 s); last save `step-20000.pth` (loss 3.16 at save, 2.9–3.2 band since step 10k, 1.33 B of the 1.69 B-token horizon), kept as the R3 init candidate; `nan-rwkv-0.pth` deleted; supervisor killed so it cannot resume. **R1 v2 finished:** held-out top-1 16.40 % at step 20k (top-5 39.4 %, top-1-or-neighbour 26.4 %; layer weights L12/16/20/23 = 0.15/0.12/0.51/0.22), flat from step 17k → matches v4 (16.1 %) with the same ~5k tracks: exact match is data-bound, not model-bound. `yue2_prep_real.py --shard 1` done (824 songs, 2,875 s). Launched: joint teacher from v2 on real shards 0–1 (GPU 0, `out/joint/v2`), v2 round trip + eval on the 20 real songs (GPU 1), minting 2 × 300 songs from suno shards 2–3 (GPUs 2–3). Disk 146 GB free is the binding constraint (MERT features 38 MB/song) → next engineering item is online MERT in the joint trainer so all 94k real songs become usable with latents only.
- **2026-09-15 21:50 v2 fails on real audio.** 20-song round trip with the v2 head (+ v4 NAR LoRA, 32 steps): WER vs lyrics **0.70** (v4 head 0.48, v1 0.52), WER vs the original's transcript 0.67 (0.44), MERT cosine **0.933** (0.968), token repeat 0.134. Joint teacher from v2: real held-out flow loss 1.216 at step 0 (v1: 1.148) → 1.022 at step 400. So minted-val top-1 (16.4 %, = v4) says nothing about real audio; minted-only training overfits the minted feature domain. Decision recorded in §8.6: heads are selected by the real round trip + real flow loss; the joint teacher is the main R1 objective; control joint run from the v1 init launched on GPU 1 (`out/joint/v1b`). Minting on a dedicated GPU runs at ≈ 50 s/song (2.5 min beside the trainer), so the 2 × 300 batch finishes ~01:30.
- **2026-09-16 00:40 R1 gate passed: joint v2 beats the v4 head on unseen real audio.** Both joint runs finished (6k steps, ~1.9 h each; real held-out flow loss v2-init 1.216 → 0.991, v1-init 1.163 → 1.014). Evaluated three ways (table in §8.6): shard-0 20 songs (in the joint training set), **shard 2 tar re-downloaded as an unseen real test set** (20 songs), and 10 fresh minted songs from tonight's minting (`data/yue2_minted_heldout10/`, symlinks; never seen by any head). Joint v2: WER vs the original's transcript 0.29 / 0.33 / 0.38 on the three sets vs v4's 0.44 / 0.50 / 0.38, MERT cosine 0.980 / 0.981 / 0.994 vs 0.968 / 0.971 / 0.994; true-token ceiling on the minted set 0.35 / 0.996. **R1-a := `out/joint/v2/head_best.pt` + `out/joint/v2/lora_best.pt`.** Minting: 152 + 142 of 246 + 262 songs done at 23:40, ≈ 50 s/song. Disk 138 GB free.
- **2026-09-16 08:30 G1 running with R1-a; online-MERT teacher built; R3 data path ready.** Minting done (772 own minted songs). **G1** (`scripts/prepare_suno94k_yue2.py`, 2 workers, GPUs 0–1, shards 0–42 / 43–84, ~13 min per shard at 160× realtime, network not limiting): shard 0 done (1,135 songs, 64 h, tokens 11 MiB + latents 707 MiB), shards 1 and 43 in progress; tars for shards 0–2 kept (`--keep-shards`). Findings that changed the plan (details §8.6 "Online MERT"): MERT on VAE-decoded latents gives only 9–14 % token agreement with real-audio features, so latents alone cannot feed the teacher; MERT on ~80 kbps opus keeps 80 % agreement at 2.3 MB/song → G1 now also writes `shard-NNNNN.audio/<id>.opus` for shards 1–18 and 43–60 (~40k real songs, ~95 GB). New `scripts/online_mert.py` (GPU MERT from opus/latent, `MemTrack`) and `scripts/train_joint_online.py` (joint teacher over stored + online real songs, producer thread, rescans G1 output); **v3 running on GPU 2** from R1-a (`out/joint/v3`, 20k steps, LR 3e-5, `logs/train_joint_v3online.log`), stored-feature phase until the first opus shard lands. Deleted stored MERT features of corpus tracks 1,501–4,720 (111 GB); disk 225 GB free. **R3 data path:** `scripts/yue2_layout.py` (vocab 98,816: YUE2CODEC 65540, codes at 65,544 + code), `scripts/build_yue2_binidx.py` (manifest + binidx from G1 shards, shard 2 excluded as test; smoke on shard 0: 1,006 kept of 1,135, docs p50 5,352 / p95 7,506 / max 8,175 at ctx 8,192, 103 songs > 300 s dropped), `scripts/resize_vocab_yue2.py` → `out/stage1_yue2/rwkv-init.pth` (from `step-20000.pth`, rows < 65,544 kept, code rows re-initialised, 3.118 B params). Single-GPU smoke of RWKV-LM at vocab 98,816 / ctx 8,192: the patched head-CE kernel compiled for the new vocab and forward/backward ran; the run then OOM-ed in DeepSpeed's optimizer step, which is the expected 1-GPU ZeRO-2 limit for a 3 B model (25 GB of Adam state), not a kernel problem — the 4-GPU G3 launch is the real test.
- **2026-09-16 21:30 G1 done, G3 launched; v3 online-MERT head does not beat R1-a.** G1 finished all 85 shards at ~21:20 (3 workers, 24–25 min/shard, 156–160× realtime; 93,058 songs, 0–2 failures per shard; opus stored for shards 1–18 and 43–60, 36 × 2.2 GiB). **v3** (`out/joint/v3`, R1-a init, 20k steps, LR 3e-5, online MERT from opus, pool grew to 21,994 songs, 11,190 online + 3,866 stored real steps): held-out real flow loss 0.9909 → 0.9818, but on the 20 unseen shard-2 songs it is *worse* than R1-a: WER vs original 0.40 vs 0.33 (vs lyrics 0.58 vs 0.52), cosine 0.979 vs 0.981 (`logs/yue2_roundtrip_s2_jv3.log`). Reading: 20k more steps on 13× more real audio bought 0.009 flow loss and no round-trip gain; the head is not limited by the amount of real audio at this model size / this objective. Candidates for the next R1 iteration (not now): larger head, higher LR for the head alone, opus at ~130 kbps (96 % token agreement), or a real-audio CE target from R1-a pseudo-labels. **R1-a stays.** **R3 data:** `scripts/build_yue2_binidx.py --ctx 8192 --test-shards 2` over 84 shards → 93,058 songs, kept 82,671 (dropped 8,663 > 300 s, 806 < 30 s, 880 over ctx), train **81,910 docs / 427.9 M tokens / 4,381 h** (11,606 instrumental, 47,667 creators), val 750 docs / 3.9 M tokens (11 lyric-leak removals), doc length p50 5,360 / p95 7,547, magic_prime 52,223; 168 s to build. **G3 launched 21:30** with `scripts/train_stage1_yue2.sh 0,1,2,3 3`: init `out/stage1_yue2/rwkv-init.pth`, ctx 8,192, LR 3e-5 → 3e-6, warmup 500, 3 epochs = 1.28 B tokens, step saves every 500, supervisor auto-resume; log `logs/train_stage1_yue2_20260916_213031.log`, per-step `out/stage1_yue2/step_log.txt`. **Disk:** root 94 GB free; NFS `/models` (2 TB, 536/577 MB/s write/read) mounted; copy-only backups (`scripts/copy_to_models.sh`, log `logs/copy_to_models.log`) of `out/stage1`, `data/tokens`, tars 0–1, `data/yue2_real` verified, `data/yue2_minted*` in progress. Deleting the root-disk originals (≈ 190 GB) needs the user's go-ahead.
- **2026-09-18 06:10 G3 finished: `out/stage1_yue2/rwkv-final.pth`.** 39,170 steps × 4 × 8,192 = 1.28 B tokens (3 epochs) in 32.7 h at a steady 11.0–11.2 k tok/s, **no NaN kill** (LR 3e-5 → 3e-6 with warmup 500; the cb0 run had three at 1e-4…2e-5, so either the lower peak or the new token space fixed it — no grad-norm logging was added, so the trigger stays unexplained). Loss 9.1 (step 130) → 4.4 (4k) → 3.9 (16k) → 3.6 (32k) → **3.54–3.65 at the end** (nats/token over the whole document, text + 32,768-way codes; the cb0 model ended at ≈ 3.0 over 1,024-way codes, so the numbers are not comparable). Epoch saves `rwkv-0/1/2.pth`, step saves every 500 (last two kept), `step_log.txt` has every 10th step. The trainer's NCCL teardown printed `DistBackendError` traces after `rc=0`; harmless (the supervisor saw `rwkv-final.pth` and stopped). GPUs were idle from 06:11 to 10:00 because nothing was queued behind the run. Backups of `rwkv-final/0/1/2.pth`, the binidx and the whole G1 corpus (139 GB) to `/models/rwkv-music/` started 10:00 (`logs/copy_to_models.log`). Root disk 52 GB free.
- **2026-09-18 10:20 G3 gate started: generation + render tooling.** `scripts/rwkv_generate.py` (rwkv_py312 env): loads the .pth like `rwkv_v7_demo_fast.py` (fp16, transposed matrices, ln0 folded into emb, `wkv7s` CUDA kernel for the prompt prefill, RNN step for decoding), prompt = `world_tokens("[Genre] caption\n[Lyrics]\nlyrics\n") EOD SOA YUE2CODEC`, samples only the 32,768 code rows (+ EOA after `--min-frames`), top-p 0.95, T 1.0, writes `<out>/<id>/{semantic.npy, request.json}` in the minted-song layout. Smoke: 300 codes in 24 s (**12.4 tok/s on one A100 in this pure-PyTorch RNN loop**, i.e. 0.5× realtime at 25 Hz — fine for evaluation, a WKVM/CUDA-graph decode path is needed for the R6 streaming goal). `tools/yue2_render_tokens.py` (venvs/yue2): NAR (+ R1-a's `lora_best.pt`, merged) + VAE → `audio_<tag>.flac`, and copies into `out/yue2_roundtrip_s2/<id>/C_roundtrip_<tag>.flac` so `yue2_roundtrip_eval.py` scores RWKV songs against the real Suno originals with the same WER/MERT-cosine protocol (WER vs lyrics is the meaningful one here: the generated song is a *new* performance of the same lyrics, so WER vs the original's transcript only measures style/voice closeness). Running: 20 shard-2 prompts (the same songs as the R1 test set), 4 GPUs × 5 prompts, `out/gen/final_s2`, logs `logs/rwkv_gen_final_s2_p{0-3}.log`.
- **2026-09-18 12:30 G3 gate: NEGATIVE on lyric adherence, partial on style.** 20 songs from `rwkv-final.pth` (T 1.0, top-p 0.95): 13/20 end with EOA on their own, mean 201 s, token repeat 0.23 (real R1-a tokens 0.11), one collapsed (repeat 0.87); unigram entropy 12.1 bits vs 13.2 real, top-10 codes carry 16 % of mass vs 3 % real. Rendered through NAR + R1-a LoRA + VAE and scored against the real Suno songs (`logs/eval_final_s2.log`): **WER vs lyrics 1.37 (median 1.05)** — the sung words are unrelated to the prompt lyrics (transcripts are fluent English/Spanish/Portuguese word salad in the right language and style, e.g. Portuguese prompt → Portuguese-ish vocals) — vs 0.47 for the originals and 0.52 for the R1-a round trip; **MERT cosine to the original 0.80** (unrelated song 0.64, round trip 0.98): genre/style is followed, identity is not. Sampling is not the fix: T 0.7 collapses to repeat 0.999 (6 unique codes), top-k 50 gives repeat 0.28. **Teacher-forced diagnostic** (`scripts/rwkv_eval_loss.py`, 40 val docs): code-token CE **4.21 with the matched text, 4.31 with another song's text, 4.23 with no text**; text-token CE 3.04; code CE by position 4.25 / 4.25 / 3.45 (first 250 / middle / last 250 frames). Uniform over 32,768 codes would be 10.4 nats, so the model learned a strong *unconditional* code LM (and uses the caption a little: a wrong caption costs 0.08 nats) but almost nothing of the lyric content: the whole-song format puts each lyric line thousands of tokens before the frames that sing it, and 1.3 B tokens are not enough for a 3 B model to learn that binding through 32k-way noisy tokens. YuE1 solved exactly this with **section-interleaved documents** (lyric section text immediately followed by its own audio frames) and Mothersuperior with a **lyric cursor**; both need lyric↔time alignment, which the corpus does not have (§2.3 deferred it). **Decision: R3.1 = section-aligned continued training** from `rwkv-final.pth` on the ~40k songs whose opus audio was kept (shards 1–18, 43–60), plus the whole-song docs for the rest; alignment via HeartTranscriptor timestamps + fuzzy alignment to the given lyrics (`tools/lyric_align.py`, prototype running on shard-1 opus). Reference to beat on the same 20 prompts: YuE2's own renditions (minted `data/yue2_minted_own`, being scored as variant `yue2_mint`).
- **2026-09-19 02:00 → 15:26 R3.1 trained; 2026-09-20 01:50 lyric binding appears (teacher-forced), audio gate running.** *Alignment:* the
  HeartTranscriptor ASR aligner (`tools/lyric_align.py`) ran at 3–12× realtime (word timestamps via cross-attention DTW; its segment
  timestamps are unusable — no timestamp tokens), i.e. days for 2,200 h of opus. Replaced by **CTC forced alignment against the known
  lyrics** (`tools/lyric_align_fa.py`: torchaudio MMS_FA, 1.2 GB from dl.fbaipublicfiles.com at ~100 MB/s via aria2c, cached in
  `~/.cache/torch/hub/checkpoints`; words romanised with NFKD + uroman so Cyrillic/CJK/Arabic lyrics align; a `<star>` token between
  sections absorbs ad-libs): **550–620× realtime per A100**, per-word CTC posterior kept as a quality score. Section times agree with the
  Whisper word-timestamp reference to a few seconds on the 5 prototype songs; the Russian song Whisper failed on (coverage 0) aligns at
  score 0.35. **All 36 opus shards, 40,047 songs, aligned in ~1.5 h on 4 GPUs** → `data/lyric_align/fa/shard-NNNNN.jsonl` (5,010 songs
  without words, 8 with more characters than frames; score p5/p50/p95 = 0.04 / 0.18 / 0.40). *Corpus* (`scripts/build_yue2_sec_binidx.py`,
  new control id **SOS 65541** in `yue2_layout.py`): `header EOD (SOS "[marker]\nstanza" SOA YUE2CODEC codes EOA)* 0`; sections = [marker]
  blocks split at blank lines, boundary = midpoint between aligned neighbours, unaligned markers ±0.5 s, < 1 s sections merged forward;
  accepted if score ≥ 0.04, ≥ 80 % words romanised, no section squeezed under 0.12 s/word. Unaligned songs (no opus, instrumental, the
  other 47 shards) keep the G3 whole-song layout for a 30 % id-hash subset. `data/binidx/yue2_sec_stage1_train`: **42,719 docs, 233.1 M
  tokens (26,029 section docs = 145.3 M, 62 %), 2,261 h, magic_prime 28,439**; val 377 docs (242 section); median 7 sections/song,
  section length p10/p50/p90 = 4.2 / 15.7 / 41.3 s; 2,114 aligned songs dropped for exceeding ctx 8,192 with the extra text.
  *Training:* `scripts/train_stage1_sec.sh 0,1,2,3 2` → `out/stage1_sec/` (init = symlink to `out/stage1_yue2/rwkv-final.pth`), LR
  2e-5 → 2e-6 cosine, warmup 500, 2 epochs = 466 M tokens = **14,230 steps in 13.4 h, no NaN kill**, `out/stage1_sec/rwkv-final.pth`
  (step checkpoints deleted, disk was at 99 %: 17 GB free → 29 GB). 24.3 GB/GPU; 4.5 k tok/s while sharing with the ROSA adapter runs,
  11 k tok/s alone. Loss 3.6–4.0 at start (G3 band; the SOS/text tokens are new) → 3.0–3.4 in the last 1k steps.
  *Teacher-forced gate* (`scripts/rwkv_eval_loss_sec.py`, 40 val section docs, 4 prompts: matched / shuffled = another song's texts /
  permuted = own section texts rotated / none):

  | model | matched | shuffled | permuted | none | shuffled−matched | **permuted−matched** | first-50-frames matched / permuted | SOS/EOA CE |
  |---|---|---|---|---|---|---|---|---|
  | G3 `stage1_yue2/rwkv-final` | 4.540 | 4.680 | 4.540 | 4.447 | 0.14 | **0.0005** (frac>0 0.47) | 5.37 / 5.37 | 24.7 |
  | R3.1 `stage1_sec/rwkv-final` | 4.315 | 4.551 | 4.421 | 4.396 | 0.24 | **0.106** (frac>0 1.00) | 4.42 / 4.62 | 1.78 |

  G3 had zero local binding (permuted = matched to 4 decimals). R3.1 pays 0.11 nats/frame for the wrong stanza on every one of the
  40 songs, 0.20 in the first two seconds of a section, and having *no* text is now worse than the right text (G3: better). The
  binding is real but modest (the whole-song header still carries the style; the stanza carries the words). *Generation:*
  `scripts/rwkv_generate.py --sections 1` decodes stanza by stanza (header EOD, then SOS text SOA YUE2CODEC … EOA per stanza,
  `--min/max-sec-frames` 50/1500). 20 shard-2 prompts → `out/gen/r31_s2_sec` (4 GPUs, ~13 tok/s each): songs end with EOA per
  section on their own, 94–170 s, **token repeat 0.07–0.11 (real 0.11, G3 0.23)**. Rendering (`tools/yue2_render_tokens.py --tag
  rwkv_r31`) + scoring (`yue2_roundtrip_eval.py --variants rwkv_r31_s32,rwkv_final_s32,lora_r1_jv2_s32`) run from
  `scripts/r31_render_eval.sh` → `logs/render_rwkv_r31_s2.log`, `logs/eval_rwkv_r31_s2.log`. Bar: WER vs lyrics G3 1.37, R1-a round trip 0.52,
  originals 0.47; MERT cosine G3 0.80, round trip 0.98. **Next if audio WER drops but stays > 0.8:** R3.2 = more section epochs at
  a higher LR (3e-5, the SOS/stanza rows are new), line-level sections (split at every line, not stanza), or a lyric-cursor token
  per line; if WER does not move, the binding must be enforced (CTC/ASR-in-the-loop reward or the joint teacher's flow loss).
- **2026-09-20 03:40 R3.1 audio gate: better than G3, still no lyric adherence.** 20 shard-2 prompts, section decoding (`out/gen/r31_s2_sec`:
  15/20 end with EOA, mean 163 s, 133 sections of which 117 ended by EOA and 16 hit the 60 s cap; token repeat median 0.11 but 5 songs
  0.34–0.65). Rendered (`logs/render_rwkv_r31_s2.log`, 297 s) and scored (`logs/eval_rwkv_r31_s2.log`):

  | variant | WER vs lyrics mean (median) | WER vs original transcript | MERT cos to original |
  |---|---|---|---|
  | original Suno song (A) | 0.47 (0.27) | — | 0.99 (VAE only) |
  | R1-a round trip (lora_r1_jv2) | 0.52 (0.32) | 0.33 | 0.981 |
  | G3 whole-song (rwkv_final) | 1.37 (1.05) | 1.37 | 0.799 |
  | **R3.1 section (rwkv_r31)** | **1.05 (0.97)** | 1.09 | 0.791 |
  | unrelated song | — | — | 0.64 |

  R3.1 beats G3 on 15/20 songs and removes the catastrophic ones (G3 had 3.40 / 2.46 / 2.02 / 1.86; R3.1 max 1.79), but a WER ≈ 1.0 still
  means the sung words are not the prompt's: distinct content words of the transcript that occur in the lyrics = **10 % (R3.1) vs 7 % (G3)
  vs 80 % (original)**; the vocals are fluent word salad in the right language. Style unchanged (cos 0.79 ≈ G3). So the 0.11-nat
  teacher-forced binding is real but far too weak to steer 32k-way sampling; consistent with YuE1's experience that lyric following
  needed the CoT format *from the start of training* plus far more data (YuE1/YuE2 trained on hundreds of thousands to millions of songs; exact count not verified here, and not available to us in any case). **Decision for R3.2** (not
  launched): (a) line-level interleaving (SOS per lyric line, ~2–6 s of audio each, from the same alignment — `lines` in the fa jsonl)
  to shrink the text→audio distance by another 5×; (b) LR 3e-5 and 3–4 epochs, since the loss was still falling (3.0–3.4) at the
  2e-6 tail; (c) drop the whole-song docs entirely so 100 % of the budget is section format; (d) if (a)–(c) do not move the
  transcript-overlap above ~30 %, the binding has to be enforced with an objective — an ASR (HeartTranscriptor) or CTC-posterior
  reward on rendered audio, or the joint teacher's flow loss through NAR+VAE — rather than more LM tokens. Disk: 28 GB free
  (out/stage1_sec holds rwkv-final, rwkv-0, rwkv-onepass = 18.6 GB; rwkv-0 and rwkv-onepass can go once the user agrees).
